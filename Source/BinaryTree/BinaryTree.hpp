

namespace binaryTree{

/* Binary tree

Example of simple binary tree node. In binary tree each node can have only 2 child (up to) - left child and right child.
*/

class Node
{
public:
    int key;
    Node* leftChild;
    Node* rightChild;
};


}